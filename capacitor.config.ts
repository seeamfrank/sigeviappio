import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.sigeviapp.com',
  appName: 'sigevi',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
